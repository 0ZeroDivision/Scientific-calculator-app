// preload.js — runs in renderer before page scripts, isolated from Node
// Nothing needed here; main.js handles all injection via webContents APIs.
window.addEventListener('DOMContentLoaded', () => {});
